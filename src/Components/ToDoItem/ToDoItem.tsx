import React from 'react'
import { IToDoItemProps } from './types'

export function ToDoItem({
    status,
    text,
    timeStamp,
    title,
}: IToDoItemProps) {
  return (
    <div>ToDoItem</div>
  )
}
